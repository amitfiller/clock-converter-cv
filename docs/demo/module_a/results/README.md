# Module A — results folder / תוצאות

**EN:** Place **screenshots** here (e.g. training curves, confusion snippets, or terminal captures of `python scripts/eval_module_a_val.py`). The numeric summary of one training run is also recorded in `docs/module_a_summary.md`.

**HE:** שימרו כאן **צילומי מסך** (עקומות אימון, פלט טרמינל של `eval_module_a_val.py` וכו'). מספרים מפורטים מהאימון מופיעים גם ב־`docs/module_a_summary.md`.

## Suggested captures / מה לצלם

- Terminal output of: `python scripts/eval_module_a_val.py`
- Optional: last epochs from `python -m src.module_a.train` (if re-run)
